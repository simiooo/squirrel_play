/// Enum representing different metadata source types.
enum MetadataSourceType {
  /// Data from local Steam manifest files.
  steamLocal,

  /// Data from Steam Store Web API.
  steamStore,

  /// Data from RAWG API.
  rawg,
}
