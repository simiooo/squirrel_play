/// Application-wide constants.
class AppConstants {
  AppConstants._();

  /// Application name.
  static const String appName = 'Squirrel Play';

  /// Application version.
  static const String appVersion = '0.1.0';

  /// Minimum window width.
  static const double minWindowWidth = 400.0;

  /// Minimum window height.
  static const double minWindowHeight = 300.0;

  /// Default window width.
  static const double defaultWindowWidth = 1280.0;

  /// Default window height.
  static const double defaultWindowHeight = 720.0;
}
